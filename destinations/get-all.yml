info:
  name: get-all
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/destinations"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
