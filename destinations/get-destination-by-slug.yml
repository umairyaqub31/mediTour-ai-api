info:
  name: get-destination-by-slug
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/destinations/colombia"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
