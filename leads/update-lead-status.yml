info:
  name: update-lead-status
  type: http
  seq: 4

http:
  method: PUT
  url: "{{base_url}}/leads/6a353b8da0f0314e7082cc3a/status"
  body:
    type: json
    data: |-
      {
        "status":"contacted"
      }



      // new, contacted, converted, closed
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
