info:
  name: get-all-leads
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/leads"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
