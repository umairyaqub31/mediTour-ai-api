info:
  name: delete-category
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{base_url}}/treatmentCategories/6a2fb0edf967a701bc333b65"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
