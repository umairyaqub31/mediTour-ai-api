info:
  name: get-passive-vendors
  type: http
  seq: 8

http:
  method: GET
  url: "{{base_url}}/vendors/passive"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
