info:
  name: get-all
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/vendors/all"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
