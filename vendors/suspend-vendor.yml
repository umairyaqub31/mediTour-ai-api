info:
  name: suspend-vendor
  type: http
  seq: 4

http:
  method: PUT
  url: "{{base_url}}/vendors/6a325d8eb8ef9146026a63bd/suspend"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
