info:
  name: get-active-vendors
  type: http
  seq: 7

http:
  method: GET
  url: "{{base_url}}/vendors/active"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
