info:
  name: approve-vendor
  type: http
  seq: 3

http:
  method: PUT
  url: "{{base_url}}/vendors/6a325d8eb8ef9146026a63bd/approve"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
