info:
  name: login
  type: http
  seq: 5

http:
  method: POST
  url: "{{base_url}}/auth/login"
  body:
    type: json
    data: |+
      
      {
        "email": "superadmin@meditour.pk",
        "password":"ChangeMe@123"
      }

  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
