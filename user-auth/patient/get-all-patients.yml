info:
  name: get-all-patients
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/superadmin/patients"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
