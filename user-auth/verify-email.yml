info:
  name: verify-email
  type: http
  seq: 3

http:
  method: POST
  url: "{{base_url}}/auth/verify-email"
  body:
    type: json
    data: |-
      {
        "email":"umairyaqub31@gmail.com",
        "otp": "341819"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
