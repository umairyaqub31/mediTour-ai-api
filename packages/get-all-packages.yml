info:
  name: get-all-packages
  type: http
  seq: 5

http:
  method: GET
  url: "{{base_url}}/treatmentPackages/admin/all"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
