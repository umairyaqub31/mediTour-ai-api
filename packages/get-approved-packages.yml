info:
  name: get-approved-packages
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url}}/treatmentPackages"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
