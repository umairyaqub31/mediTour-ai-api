info:
  name: submit-for-approval
  type: http
  seq: 2

http:
  method: PATCH
  url: "{{base_url}}/treatmentPackages/6a3249a667c3b337aaf44570/submit"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
