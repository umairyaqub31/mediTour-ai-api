info:
  name: approve-package
  type: http
  seq: 3

http:
  method: PATCH
  url: "{{base_url}}/treatmentPackages/6a3249a667c3b337aaf44570/approve"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
