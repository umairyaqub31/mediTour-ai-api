info:
  name: suspend-admin
  type: http
  seq: 3

http:
  method: PUT
  url: "{{base_url}}/superadmin/admins/id/suspend"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
