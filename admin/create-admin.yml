info:
  name: create-admin
  type: http
  seq: 1

http:
  method: POST
  url: "{{base_url}}/superadmin/admins"
  body:
    type: json
    data: |-
      {
        "name": "Ahmed Khan",
        "email": "admin1@example.com",
        "phone": "+923001234568",
        "password": "Admin@123",
        "role": "admin"
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
