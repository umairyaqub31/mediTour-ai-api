info:
  name: active-admin
  type: http
  seq: 4

http:
  method: PUT
  url: "{{base_url}}/superadmin/admins/6a2bf53b625fb69fdcb378c6/activate"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
